A small follow-up to the last release, fixing search in sources built with
RepoForge.

RepoForge
- Search now works in generated sources for sites that lay out their results
  page differently from their browse page. Only the browse layout was being
  recognised, so a generated source could list titles perfectly and then return
  nothing at all for every search. Webtoons was the clearest example.
